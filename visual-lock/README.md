# Visual lock baselines

Canonical approved screenshots live in `baselines/`. How they may be updated, who must approve an update, and how release failure works: `docs/engineering/VISUAL_DESIGN_LOCK.md`.

Never overwrite these PNGs because implementation changed.
